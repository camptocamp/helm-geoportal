annotations:
  reloader.stakater.com/auto: 'true'

alembic:
  annotations:
    reloader.stakater.com/auto: 'true'

tools:
  annotations:
    reloader.stakater.com/auto: 'true'

ingress:
  enabled: true
  hosts:
    - host: gmf.local
      paths:
        - /

serviceAccount:
  create: true
  name: full
